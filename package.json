{
  "name": "ai-stream-world",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel deploy"
  },
  "dependencies": {
    "@vercel/kv": "^0.2.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "vercel": "^32.0.0"
  }
}